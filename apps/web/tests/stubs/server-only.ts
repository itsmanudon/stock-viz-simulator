// Stub for the `server-only` package under Vitest. The real module throws when
// imported outside a React Server Component; these tests exercise the module
// logic directly, where that guard isn't meaningful.
export {};
