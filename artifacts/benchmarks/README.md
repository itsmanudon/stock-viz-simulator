# Benchmark outputs

`scripts/k8s/run-benchmark.sh` writes `kafka-scaling.json` here after a real
kind/CI run. Those files are gitignored so nobody can commit invented
throughput numbers.

The methodology and (when a run happened) interpretation live in
[`docs/KAFKA_SCALING.md`](../../docs/KAFKA_SCALING.md).
